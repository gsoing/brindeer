_format_version: "2.1"
_transform: true

services:
  - name: profile
    url: http://profile:8080/api
    plugins:
      - name: key-auth
    routes:
      - name: profile
        paths:
          - /test

consumers:
  - username: merveille
    keyauth_credentials:
      - key: merveille
